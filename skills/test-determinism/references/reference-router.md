# Reference Router

Read first. Load one more unless blocked.

## Route

- Need symptom -> cause map: load `nondeterminism-catalog.md`
- Need fixtures, pollution, parallelism: load `isolation-and-parallelism.md`

## Stop Condition

Stop when answer can provide:
1. likely nondeterminism class
2. concrete fix or confirming experiment
3. quarantine/retry verdict and risk
4. if API doubles vs real service mismatch, point to `api-contract-critic`
