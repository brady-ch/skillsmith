[[references]]
file = "repo-briefing.md"

[references.metadata.trigger]
summary = "Use for the request is primarily about understanding the repository and producing an implementation brief."
intent_tags = ["repo", "brief", "analysis"]
when_to_use = ["Summarize repository state before coding.", "Produce an implementation brief."]

[references.metadata.objective]
summary = "Produce a concise repository briefing with architecture facts and change points."

[references.metadata.output]
summary = "Return a current-state summary, implementation direction, and one concrete risk."

[references.metadata.navigation]
summary = "Prefer this reference for discovery and briefing requests."
priority = 10

[[references]]
file = "risk-triage.md"

[references.metadata.trigger]
summary = "Use for the request is focused on prioritizing risks, blockers, or mitigation work."
intent_tags = ["risk", "triage", "blocker", "mitigation"]
when_to_use = ["Rank technical risks.", "Provide mitigation steps for likely failures."]

[references.metadata.objective]
summary = "Rank findings by severity and translate them into concrete mitigation actions."

[references.metadata.output]
summary = "Return severity-ranked findings, why they matter now, and validation checks."

[references.metadata.navigation]
summary = "Prefer this reference when the user asks for strict prioritization instead of general discovery."
priority = 20
